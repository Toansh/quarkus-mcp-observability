apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-automater.fullname" . }}
  labels:
    {{- include "github-automater.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "github-automater.selectorLabels" . | nindent 4 }}
